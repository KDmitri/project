package org.example;

public class User {
    public boolean checkAge(){
        int age = 0;
        if (age > 18 & age < 100);
        return true;
    }
    public boolean checkPassword(){
        String letter;
        int symbols;
        if (letter.matches("A-Z") & symbols > 8);

        return true;
    }
}
